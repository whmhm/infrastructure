{
  "name": "@infrastructure-monorepo/api-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "clean": "rm -rf .turbo dist node_modules",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "@infrastructure-monorepo/tsconfig": "workspace:*",
    "tsup": "^6.0.0",
    "typescript": "^5.0.0"
  }
}